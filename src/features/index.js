import ListRegisters from './ListRegisters'
import Register from './Register'

// Agrupa as features (telas/páginas) para exportar
const Features = {
    ListRegisters,
    Register
}

// Exporta um objeto contendo todas as features
export default Features