const theme = () => ({
    viewContent: {
        height: '100%',
        display: 'flex',
        flexDirection: 'column'
    },
    content: {
        marginTop: 80,
        flex: '1 0 auto',

        // Configuração para alinhar o conteúdo no meio verticalmente (opcional)
        display: 'flex',
        flexDirection: 'column',
        justifyContent: 'center'
    }
})

export default theme