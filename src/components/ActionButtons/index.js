import ListRegisters from "./ListRegistersActionButton"
import Register from "./RegisterActionButton"

// Agrupa os botões de ação para exportar
const ActionButtons = {
    ListRegisters,
    Register
}

// Exporta um objeto contendo todos os botões
export default ActionButtons